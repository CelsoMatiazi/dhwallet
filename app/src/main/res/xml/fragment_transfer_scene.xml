<?xml version="1.0" encoding="utf-8"?>
<MotionScene 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">

    <Transition
        motion:constraintSetEnd="@+id/end"
        motion:constraintSetStart="@id/start"
        motion:duration="1000">
       <KeyFrameSet>
       </KeyFrameSet>
        <OnSwipe motion:touchAnchorId="@id/recycler_transfer" />
    </Transition>

    <ConstraintSet android:id="@+id/start">
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end">
        <Constraint
            android:id="@+id/guide_header"
            android:layout_width="0dp"
            android:layout_height="0dp"
            motion:layout_constraintGuide_begin="65dp" />
        <Constraint
            android:id="@+id/search"
            motion:layout_constraintEnd_toEndOf="@id/guide_end"
            android:layout_width="0dp"
            android:layout_height="40dp"
            motion:layout_constraintTop_toTopOf="parent"
            motion:layout_constraintBottom_toBottomOf="@id/guide_header"
            motion:layout_constraintStart_toStartOf="@id/guide_start"
            motion:layout_constraintTop_toBottomOf="@+id/background_header"
            android:layout_marginEnd="36dp"
            android:layout_marginStart="36dp"
            android:layout_marginBottom="5dp" />
    </ConstraintSet>
</MotionScene>